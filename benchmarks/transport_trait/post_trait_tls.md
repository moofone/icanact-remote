# Post-Trait TLS Benchmark Report

Historical benchmark note from an earlier transport-trait migration.

What still maps to the current tree:

- `tests/integration/throughput_benchmarks.rs` exists and prints throughput lines for tell and ask actor-frame tests.

What does not map cleanly anymore:

- the raw artifact directories referenced in the original report are not present
- the larger parity-gate and transport-trait harness described by the old report is not present in this checkout

Treat this file as archived context, not as a current reproducible performance report.
