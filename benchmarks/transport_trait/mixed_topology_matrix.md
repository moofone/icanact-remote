# Mixed Topology Matrix

Archived note from a previous transport experiment.

The current checkout does not contain the mixed-topology test suites or raw artifact paths referenced by the older matrix, so this file should not be treated as a reproducible benchmark report for the present repository state.
