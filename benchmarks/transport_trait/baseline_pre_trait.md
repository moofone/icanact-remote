# Pre-Trait TLS Baseline

Historical baseline note.

The pre-trait raw artifacts referenced by the original report are not present in this checkout, so this file is archival only and should not be treated as a currently reproducible benchmark source.
