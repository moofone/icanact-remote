# Transport Trait Edge-Case Coverage

This file is an archived benchmark/planning note from an earlier transport-trait effort.

It does not match the current checkout:

- the referenced QUIC, Noise, UDP mixed-topology, and `transport_edge_cases` test files are not present
- the referenced `src/noise_auth.rs` module is not present

Use the current `tests/` directory as the source of truth for what is actually covered today.
