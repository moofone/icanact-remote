# Per-Transport Matrix

Archived transport comparison note.

The current checkout does not include the `TcpNoiseStack` or `UdpStack` benchmark setup described by the older matrix, so this file should be read as historical context rather than current benchmark evidence.
